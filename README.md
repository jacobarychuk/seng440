Compile and run the project with:

`gcc main.c sad.c -o main`

`./main`

To inspect the generated assembly code for the two SAD implementations, stop compilation after generating the assembly code:

`gcc -S sad.c`
